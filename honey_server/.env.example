# ---- Honeypot Server .env ----

PORT=8080

# URL of the Monitor Server (private IP or DNS in AWS)
# e.g. MONITOR_URL=http://10.0.3.10:9000
MONITOR_URL=http://localhost:9000

# Shared secret for log forwarding (must match monitor_server)
LOG_SECRET=honey_internal_secret
